# Django Example

Django integration is intentionally not implemented in the core package yet.

Future Django support may provide forms, views, or model fields for saving
prompt-building sessions. For now, install and use `prompt_recipe_smith` from ordinary
Python code inside your Django project.
