Installation
============

Install the package from PyPI:

.. code-block:: bash

   uv add prompt-recipe-smith

You can also install it with ``pip``:

.. code-block:: bash

   python -m pip install prompt-recipe-smith

The package targets Python 3.12 and has no runtime dependencies.
