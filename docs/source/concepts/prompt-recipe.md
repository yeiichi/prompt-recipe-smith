# PromptRecipe

A `PromptRecipe` is a reusable conditional prompt-building procedure.

It can contain metadata, steps, examples, defaults, and up to three branches.
It is useful when the user starts from a vague idea and needs help shaping a
better request.
